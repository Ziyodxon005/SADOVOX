/**
 * VideoVoice — Frontend Konfiguratsiyasi
 *
 * Faqat UI sozlamalari shu yerda.
 * API key .env faylida (server tomonida) saqlanadi.
 */

export const CONFIG = {
  // Standart tarjima tili
  DEFAULT_LANGUAGE: 'uz',
};
